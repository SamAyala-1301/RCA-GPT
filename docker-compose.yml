version: '3.8'

services:
  rca-gpt:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run dashboard.py --server.address 0.0.0.0
